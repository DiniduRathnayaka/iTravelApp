<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.itravel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".HotelViews" />
        <activity android:name=".LocationsDelete" />
        <activity android:name=".LocationsAdd" />
        <activity android:name=".HotelsAdd" />
        <activity android:name=".LocationsUpdate" />
        <activity android:name=".HotelsDelete" />
        <activity android:name=".HotelsUpdate" />
        <activity
            android:name=".LocationsView"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>